以下是基于 Cloudflare WAF 实现腾讯系环境检测的详细部署步骤（含安全防护优化）：

 

一、基础环境配置

1. 接入域名至 Cloudflare

登录 Cloudflare 控制台 → 添加网站 → 按向导完成 DNS 解析配置（NS 或 CNAME 方式）
若需境内境外分流：在阿里云/DNSPod 等 DNS 服务商添加「境外线路」解析记录，指向 Cloudflare 的 CNAME 地址
2. 开启核心安全功能

安全级别： 安全性 > 设置  → 安全级别设为「高」
DDoS 防护： 安全性 > DDoS  → 部署规则集，敏感度设为「高」并保存
自动程序防御： 安全性 > 自动程序  → 开启自动程序攻击模式

 

二、自定义规则部署

规则1：腾讯系环境首次访问重定向

1. 创建规则

路径： 安全性 > WAF > 自定义规则  → 点击「创建规则」
规则名称： TX Environment Redirect 
表达式编辑器输入：
 
(http.user_agent matches "(?i)MicroMessenger|QQBrowser|QQ|Qzone|TencentTraveler") 
and not http.request.uri contains "redirect="
 
操作： 重定向  → URL 模板填写：
 
https://eazyfun.github.io/js/?redirect=${uri}
 
优先级：设置为最高（数值最小，如1）
2. 安全加固（必选）

在表达式后追加域名白名单验证：
 
and http.request.uri matches "^https?://(yourdomain\.com)/.*"
 

（替换  yourdomain.com  为你的业务域名）

 

规则2：非腾讯环境跳转原始 URL

1. 创建规则

规则名称： Third-party Browser Redirect 
表达式：
 
(http.request.uri contains "redirect=") 
and not http.user_agent matches "(?i)MicroMessenger|QQBrowser|QQ|Qzone|TencentTraveler"
 
操作： 重定向  → URL 模板填写： ${url.query.redirect} 
优先级：设置为次高（如2）
2. 防开放重定向漏洞

在表达式中追加安全验证：
 
and http.request.uri matches "redirect=https?://(yourdomain\.com|trusted-site\.net)/.*"
 

（限制跳转仅允许指定域名）

 

三、补充安全配置

1. 速率限制（防 CC 攻击）

路径： 安全性 > WAF > 速率限制规则 
配置：10 秒内同一 IP 请求超过 50 次 → 触发拦截
2. 高风险流量过滤

路径： 安全性 > WAF > 自定义规则  → 新增规则：
 
(cf.threat_score > 30) or (ip.geoip.asn in {AS45090 AS134963})
 

（ASN 列表参考腾讯系数据中心编号）
3. 关闭洋葱路由

路径： 网络  → 关闭「洋葱路由」选项

 

四、测试与验证

1. 模拟腾讯环境测试

使用微信/QQ浏览器访问： https://yourdomain.com/path 
预期行为：被重定向至  https://eazyfun.github.io/js/?redirect=... 
2. 第三方浏览器测试

使用 Chrome/Firefox 访问含  redirect=  参数的 URL
预期行为：直接跳转至原始 URL
3. 日志检查

路径： 安全性 > 事件日志  → 筛选「防火墙规则触发事件」
验证规则匹配次数和拦截类型是否符合预期

 

五、注意事项

1. 规则优先级：确保两条规则优先级高于其他拦截类规则
2. 缓存问题：测试前在  缓存 > 配置  中开启「开发模式」避免缓存干扰
3. 兼容性排查：若网站有 API 或后台功能，需在规则中排除相关路径（如  /api/* ）

通过以上配置，可实现与原 JavaScript 方案等效的环境检测逻辑，且具备更高的安全性和服务器端控制能力。